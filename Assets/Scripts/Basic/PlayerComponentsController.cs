using UnityEngine;

public class PlayerComponentsController : MonoBehaviour
{
    /*[SerializeField] private MoveController moveController;


    private void Update()
    {
        KeyHandle();
    }



    public void Move()
    {
        Vector3 targetVelocity = new Vector3(Input.GetAxis("Horizontal"), 0, Input.GetAxis("Vertical"));
        moveController.MoveToward(transform.position + targetVelocity);
        if (Input.GetKeyDown(KeyCode.Space))
        {
            moveController.Jump();
        }
    }
    public void Rotate()
    {
        float yaw = transform.localEulerAngles.y + Input.GetAxis("Mouse X");
        float pitch = -Input.GetAxis("Mouse Y");

        // Clamp pitch between lookAngle
        pitch = Mathf.Clamp(pitch, -50f, 50f);

        transform.localEulerAngles = new Vector3(pitch, yaw, 0);
        
    }
    
    public void SetMovementController(MoveController controller)
    {
        moveController = controller;
    }
    
    void KeyHandle()
    {
        if(Input.GetKeyDown(KeyCode.Space))
        {

        }
        Ge
    }
*/
}
